# КЭА — Автоматический дайджест энергетики

Система автоматической еженедельной рассылки дайджеста новостей энергетики Казахстана.
Отправляется каждый **понедельник в 09:00 (Астана)** на указанные email-адреса.

---

## Как устроено

```
GitHub Actions (бесплатный облачный сервер)
  └─ Каждое воскресенье 04:00 UTC запускает digest.py
       ├─ Собирает новости за 7 дней (RSS + скрейпинг ~10 источников)
       ├─ Фильтрует по ~30 ключевым словам энергетики
       ├─ Отправляет в Google Gemini API → структурированный дайджест
       ├─ Генерирует PDF в корпоративном стиле КЭА
       └─ Отправляет письмо с PDF через Gmail SMTP
```

**Стоимость:** $0 (Gemini API — бесплатный tier: 1500 запросов/день, нам нужно 4/месяц)

---

## Настройка — 4 шага (≈15 минут)

### Шаг 1 — Создать репозиторий на GitHub

1. Зайдите на [github.com](https://github.com) → **Sign up** (если нет аккаунта)
2. Нажмите **New repository**
3. Название: `kea-digest` | Тип: **Private** ⚠️ (важно — содержит ключи)
4. Нажмите **Create repository**
5. Загрузите все файлы из этой папки в репозиторий:
   - `digest.py`
   - `requirements.txt`
   - `.github/workflows/weekly_digest.yml`
   - `assets/logo.png`

   Можно через веб-интерфейс GitHub → **Add file → Upload files**

---

### Шаг 2 — Получить Gemini API ключ (бесплатно)

1. Откройте [aistudio.google.com](https://aistudio.google.com)
2. Войдите через Google аккаунт
3. Нажмите **Get API key → Create API key**
4. Скопируйте ключ (вида `AIzaSy...`)

---

### Шаг 3 — Создать Gmail App Password

> ⚠️ **Важно:** это НЕ ваш пароль от Gmail. Это специальный пароль только для этого скрипта.

1. Откройте [myaccount.google.com/security](https://myaccount.google.com/security)
2. Убедитесь что включена **двухфакторная аутентификация**
3. Перейдите в **App passwords** (поиск по странице)
4. Выберите: App = **Mail**, Device = **Other** → введите `KEA Digest`
5. Нажмите **Generate** → скопируйте пароль (вида `xxxx xxxx xxxx xxxx`)

---

### Шаг 4 — Добавить секреты в GitHub

1. В вашем репозитории: **Settings → Secrets and variables → Actions**
2. Нажмите **New repository secret** и добавьте 4 секрета:

| Имя секрета | Значение | Пример |
|---|---|---|
| `GEMINI_API_KEY` | Ключ из Шага 2 | `AIzaSyXXXXXXXXXXXXXXX` |
| `GMAIL_USER` | Ваш Gmail адрес | `yourname@gmail.com` |
| `GMAIL_APP_PASSWORD` | Пароль из Шага 3 | `xxxx xxxx xxxx xxxx` |
| `RECIPIENT_EMAILS` | Email получателей (через запятую) | `ceo@kea.kz, pr@kea.kz` |

---

## Проверка — запустить вручную

1. Перейдите в репозиторий → вкладка **Actions**
2. Слева: **КЭА — Еженедельный дайджест энергетики**
3. Нажмите **Run workflow → Run workflow**
4. Дождитесь выполнения (~2-3 минуты) — зелёная галочка = успех
5. Проверьте ваш inbox

**Если ошибка:** нажмите на failed job → посмотрите logs → скорее всего проблема в одном из секретов.

---

## Расписание

| Событие | Время |
|---|---|
| Скрипт запускается | Воскресенье 04:00 UTC |
| Письмо приходит | Понедельник ~09:00 Астана |

Чтобы изменить время — отредактируйте строку `cron` в файле `.github/workflows/weekly_digest.yml`:
```yaml
- cron: '0 4 * * 1'   # 04:00 UTC воскресенье = 09:00 понедельник Астана
```

---

## Добавить/изменить получателей

В GitHub → Settings → Secrets → `RECIPIENT_EMAILS`:
```
ceo@kea.kz, pr@kea.kz, member@kea.kz
```

---

## Структура дайджеста

Каждый выпуск содержит:
1. ⚙ Регуляторика и госполитика
2. ₸ Тарифы и рынок
3. ⚡ ВИЭ и новые проекты
4. 🔌 Инфраструктура и надёжность
5. 🌐 Международная повестка
6. 📅 Анонсы и мероприятия
7. ⚠ **Требует реакции / позиции КЭА**

---

## Вопросы и доработки

Обратитесь к разработчику или создайте Issue в этом репозитории.

---

*Система разработана специально для ОЮЛ «Казахстанская Электроэнергетическая Ассоциация»*
*kea.kz*
